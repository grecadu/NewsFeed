@model IEnumerable<MyFeeds.Models.Feed>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Content.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContentType.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Source.Origin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Content.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContentType.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Source.Origin)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
